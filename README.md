# open62541cpp - __work in progress ...__
A C++ abstraction for open62541


### How to build?
```
git clone git@github.com:kannoth/open62541cpp.git --recursive
cd open62541cpp 
mkdir && cd build
cmake ..
```

